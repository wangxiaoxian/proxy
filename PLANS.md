# PLANS.md

## 当前阶段目标
- 交付一个最小可实现的 Charles/Fiddler 式 HTTP/HTTPS MITM 代理。
- 在测试客户端显式配置代理后，可以拦截 HTTP/HTTPS 请求并基于 profile 改写请求头。
- 支持使用多个监听端口模拟多个设备画像访问同一账号。

## 已确认的关键决策

### 1. HTTPS MITM 证书策略
- 不使用“一个固定站点证书拦截所有域名”的方案。
- 采用标准 CA 模型：
  - 首次启动生成一张本地根 CA，后续持久化复用。
  - 客户端只需要安装一次该根 CA。
  - 对每个目标域名动态签发叶子证书。
- 根 CA 不随服务重启自动轮换。
- “无限期证书”不作为目标，改为长期有效期；当前默认：
  - 根 CA：10950 天
  - 叶子证书：1825 天

### 2. 协议与流量范围
- 只支持显式代理。
- v1 支持 HTTP/1.1 普通请求和 `CONNECT`。
- v1 不支持：
  - HTTP/2
  - WebSocket
  - gRPC
  - 第三方 App 证书锁定处理

### 3. 请求头处理规则
- 同名请求头按最后值覆盖，不保留重复头。
- 请求头改写动作只支持：
  - `set`
  - `remove`
  - `append`
  - `set_if_absent`
- 改写顺序固定为：
  - 全局规则
  - profile 头部
  - 目标域名规则

### 4. Profile 选择策略
- profile 不通过运行时猜测。
- 固定采用“监听端口 -> profile”映射。
- 典型示例：
  - `8888 -> device-a`
  - `8889 -> device-b`

### 5. 域名拦截范围
- 代理允许接收所有域名请求。
- 但并非所有域名都改头：
  - 命中规则的域名执行改写
  - 未命中规则的域名仅透传并记录最小日志

## 当前实现状态

### 已完成
- 配置驱动启动流程
- 固定根 CA 生成、加载和持久化
- 叶子证书按域名动态签发与磁盘缓存
- HTTP/1.1 请求解析
- `CONNECT` + HTTPS MITM 主链路
- 按端口绑定 profile
- 按域名规则改写请求头
- 本地只读管理接口：
  - `/_admin/health`
  - `/_admin/config`
  - `/_admin/recent-requests`
- 单元测试：
  - 根 CA 重启复用
  - 请求头改写规则

### 当前已知限制
- 上游连接当前强制 `Connection: close`，还不是完整 keep-alive 代理。
- 响应当前按字节流透传，没有单独解析状态行和响应头。
- 日志当前记录的是请求维度信息，未结构化提取上游响应状态码。
- 还没有补 profile 级会话容器、Cookie 隔离或更细粒度的审计能力。

## 下一步建议

### 优先级高
- 补响应首行和响应头解析，记录真实响应状态码。
- 改造连接管理，减少对 `Connection: close` 的依赖。
- 为 profile 增加独立 Cookie / session 隔离能力。

### 优先级中
- 增加更多集成测试，包括：
  - HTTPS MITM 实际握手
  - chunked 请求转发
  - 多 profile 并发访问同一目标域名
- 增强管理接口输出，减少当前 `recent-requests` 的简化序列化形式。

### 暂不做
- HTTP/2 MITM
- WebSocket/gRPC 代理
- 绕过第三方安全机制的能力
- 图形化界面

## 变更约定
- 任何影响核心行为的设计修改，都先更新本文件，再改代码。
- 如果新增能力是面向使用者的，也要同步更新 `README.md`。
